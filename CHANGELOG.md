# 更新日志（Changelog）

本项目的版本历史与每次更新的详细说明。最新版本安装包见 [GitHub Releases](https://github.com/wayileina114-bit/p2p-chat/releases)。

格式说明：版本号采用 `主.次.修订`（SemVer）。

---

## v2.2.7（最新）

**主题：通知弹窗开关**

### ✨ 新增
- **通知弹窗开关**：「设置 → Windows 通知弹窗」可随时关闭后台新消息的系统通知（提示音仍可单独控制）

---

## v2.2.6

**主题：图片即时显示 + 图片回执**

### ✨ 改进
- **发送图片即时显示**：发出图片后自己的聊天框立刻显示图片缩略图（不再等对方接收完成才出现），QQ 式体验
- **图片也支持已送达/已读回执**：图片消息现在同样显示「已送达/已读」小标（此前只有文字消息有）

---

## v2.2.5

**主题：引用回复**

### ✨ 新增
- **引用回复**：右键任意消息点「引用回复」，输入框上方显示被引用内容，发送后消息气泡内显示「↩ 引用」预览（QQ/Discord 式）

---

## v2.2.4

**主题：成员列表**

### ✨ 新增
- **成员列表**：聊天区标题旁新增「👥 成员」按钮，点击展开当前会话在线成员列表（群聊显示所有在线成员、私聊显示对方在线状态），成员变动实时刷新

---

## v2.2.3

**主题：消息编辑**

### ✨ 新增
- **消息编辑**：右键自己的消息可「编辑」，弹出编辑框修改后同步给所有人，消息下方显示「已编辑」小标（QQ/Discord 式）

---

## v2.2.2

**主题：已送达 / 已读 双层回执**

### ✨ 新增
- **已送达回执**：消息被对方客户端收到时（无论是否打开），消息下方先显示「已送达 谁」；对方打开会话后升级为「已读 谁」（QQ 式双层回执）

### ✨ 改进
- 「已读」小标改为强调色，与「已送达」灰色小标区分

---

## v2.2.1

**主题：正在输入状态（打字提示）**

### ✨ 新增
- **正在输入提示**：对方在私聊/群聊里打字时，聊天标题会实时显示「xxx 正在输入…」（2.5 秒无动作自动消失），参考 QQ/Discord

### 🐛 修复
- **通知托盘图标残留**：Windows 通知气泡过期后自动清理托盘图标，不再留下空白图标

---

## v2.2.0

**主题：体验大修：Windows 通知 + 会话内搜索 + 流畅度/图片预览修复**

### ✨ 新增
- **Windows 原生通知**：应用在后台或最小化时收到新消息，会弹系统通知气泡（标题 + 内容预览），不用切进应用也能看到
- **会话内消息搜索**：按 Ctrl+F 打开搜索栏，输入关键词即时过滤当前会话消息（支持正文/昵称/文件名），Esc 退出

### 🐛 修复
- **版本号比较修复**：修复带 `v` 前缀的版本号（如 `v2.2.0`）被误判为旧版本，导致检查更新/版本管理排序错乱
- **图片预览修复**：超大原图（手机照片/全景）也能正常生成缩略图内联预览，不再退回「接收文件」提示
- **快速滚动残影修复**：全量渲染时不再逐条触发自动滚动与布局刷新，快速上下滑动不再出现残影/抖动
- **版本更新按钮点不到**：右侧「检查更新/主题」按钮改为优先占位，窗口较窄时也不会被左侧控件挤掉

### ⚡ 性能
- **渲染更激进**：会话渲染上限 50→200、会话列表/搜索防抖大幅缩短，界面更跟手（按「让硬件干活更卖力」重做）

### ✨ 改进
- **已读回执即时化**：正在看的会话里收到对方消息立即发送已读回执，不再等切换会话后才补发

---

## v2.1.9

**主题：会话内消息搜索 + 已读回执即时化**

### ✨ 新增
- **会话内消息搜索**：按 Ctrl+F 打开搜索栏，输入关键词即时过滤当前会话的消息（支持正文/昵称/文件名），Esc 或点 ✕ 退出

### ✨ 改进
- **已读回执即时化**：正在查看的会话里收到对方消息时立即发送已读回执，不再等切换会话后才补发

---

## v2.1.8

**主题：消息撤回 + 自动滚动彻底修复**

### ✨ 新增
- **消息撤回**：右键自己的消息可「撤回」，对方看到该消息会变成「（对方 撤回了一条消息）」

### 🐛 修复
- **自动滚动彻底修复**：新消息到达前先记录是否贴底，到达后再据此决定是否滚动，彻底解决私聊/群聊新消息不自动滚到底的问题

---

## v2.1.7

**主题：已读回执**

### ✨ 新增
- **已读回执**：自己发出的消息下方会实时显示「已读 谁、谁」，谁看过了就标谁
- **自动回执**：打开会话即视为已读，自动向对方发送已读回执（每条只发一次，群聊私聊都支持）

---

## v2.1.6

**主题：@ 提及**

### ✨ 新增
- **@ 提及**：输入框里输入 @ 会弹出在线成员列表，点选或继续输入过滤即可快速 @ 某人

---

## v2.1.5

**主题：自动滚动修复 + 激进性能优化**

### 🐛 修复
- **自动滚动修复**：滚动延迟到布局完成后再执行，私聊/群聊新消息到达、发送后都自动滚到底部

### ⚡ 性能
- **激进渲染优化**：会话切换渲染上限 150→50、会话列表刷新防抖 150ms→250ms、非当前会话消息改为防抖合并重建列表，大幅降低界面重绘开销

---

## v2.1.4

**主题：图片内联预览（低画质缩略图 + 自动接收）**

### ✨ 改进
- **图片内联预览**：收到图片时不再弹「接收文件」确认，直接显示低画质缩略图预览，后台自动接收原图并替换（QQ 式体验）
- 发图时对方即时看到缩略图，无需手动点「接收」

---

## v2.1.3

**主题：性能优化 + 自动滚动修复 + 版本管理体验**

### ⚡ 性能
- **渲染优化**：头像解码缓存、窗口标题节流、滚动区域刷新优化，减少不必要的界面重绘，聊天更流畅

### 🐛 修复
- **自动滚动修复**：显式刷新滚动区域，新消息到达/发送后自动滚到底部，不再需要手动下滑

### ✨ 改进
- **版本管理窗口更小巧**：版本选择从下拉框改为紧凑的可滚轮滚动列表

---

## v2.1.2

**主题：图片粘贴发送**

### ✨ 改进
- **图片粘贴发送**：在输入框直接 Ctrl+V 粘贴剪贴板里的图片即可发送（QQ/Discord 风格），无需先保存成文件

---

## v2.1.1

**主题：版本管理（累积更新 + 任意版本下载 + 回退）**

### ✨ 改进
- **版本管理**：「检查更新」现在打开版本管理面板：列出所有历史版本，可选择任意版本下载安装（升级或回退）
- **累积更新内容**：跨多个版本升级时，自动汇总中间所有版本的更新内容，不再只显示最新一版
- **回退旧版本**：可选择更早的版本下载回退

---

## v2.1.0

**主题：端到端加密（大更新）**

### ✨ 新增
- **端到端加密**：支持设置加密口令，文字消息与文件传输全部加密（AES 认证加密），走公共服务器也不怕被中间人偷看
- 双方使用相同口令即可互通；口令不匹配或未设置时显示「🔒 加密消息」占位
- 「设置 → 加密口令…」可随时设置 / 关闭

### 🔧 技术
- 使用 Fernet（PBKDF2-SHA256 派生 AES 密钥 + 认证加密）；`cryptography` 为可选依赖，未安装时自动退回明文模式

---

## v2.0.5

**主题：未读会话置顶 + 任务栏提醒**

### ✨ 改进
- **未读私聊置顶**：有未读消息的私聊会话自动排到列表最前面（QQ/Discord 风格）
- **新消息任务栏闪烁**：收到非当前会话的新消息时，任务栏图标闪烁提醒（窗口获得焦点后自动停止）

---

## v2.0.4

**主题：会话列表消息预览**

### ✨ 改进
- **会话列表显示最后一条消息预览**：左侧会话列表每个会话下方显示最后一条消息摘要（QQ/Discord 风格），不点进去也能看到最新动态

---

## v2.0.3

**主题：应用内更新增强**

### ✨ 改进
- **应用内更新增强**：主界面顶部新增「🔄」检查更新按钮（一键检查 + 直接下载安装）；下载新版本时状态栏实时显示下载百分比，不再只有一句「正在下载」

---

## v2.0.2

**主题：全新应用图标**

### ✨ 改进
- **全新应用图标**：重绘程序图标（蓝紫渐变圆角方形 + 白色聊天气泡 + 笑脸），替换之前单调的蓝方块；exe 主程序与安装程序都换上新图标

---

## v2.0.1

**主题：表情面板**

### ✨ 新增
- **表情面板**：输入框旁新增「😊 表情」按钮，点击弹出表情面板，点选即插入输入框，内置 100+ 常用表情（Esc 或再次点击关闭）

---

## v2.0.0

**主题：聊天消息头像 + 自动滚动修复（v2 首个大版本）**

### ✨ 新增
- **聊天消息头像**：聊天界面每条消息（消息组首条）现在显示头像——自己的用你设置的头像（圆形裁剪），对方用彩色首字母，参考 Discord 布局
- 头像仅在「同一人连续消息」的首条显示，后续消息缩进对齐，画面更清爽

### 🐛 修复
- 修复新消息到达时不自动滚动的时序问题（强制刷新布局后再判断滚动位置，不再"停在旧位置看不到新消息"）

---

## v1.9.0

**主题：私聊文件 / 图片传输（重磅功能）**

### ✨ 新增
- **私聊文件 / 图片传输**：现在可以在私聊（一对一）里直接发图片和任意文件，不再只能群聊发文件
  - 底层新增私聊专属文件通道（`dmfiles/`），与群聊文件通道完全隔离、互不影响
  - 发送 / 接收 / 拒绝 / 校验 / 流式落盘 / 进度等整套逻辑与群聊一致，同样支持 200MB 大文件
  - 收到私聊文件同样弹「接收 / 拒绝」确认卡片，结果会作为系统消息持久化

### 🔧 重构
- 文件发送逻辑抽取为统一的内部入口，群聊与私聊复用同一套发送 / 接收 / 校验流程

---

## v1.8.23

**主题：消息长度限长（安全加固）**

### 🐛 修复（安全加固）
- **消息长度限长**：收到的文字消息（昵称 / 正文）会被截断到合理长度，防止异常 / 恶意超长消息把界面或历史记录撑爆

---

## v1.8.22

**主题：记住窗口大小与位置**

### ✨ 改进
- **记住窗口大小与位置**：关闭时记住窗口的大小和位置，下次启动自动恢复，不用每次手动调整窗口

---

## v1.8.21

**主题：发送失败不再丢文字**

### 🐛 修复
- **发送失败不再丢文字**：网络在发送瞬间断开导致发送失败时，输入的文字会自动还原回输入框并提示「发送失败」，不再静默丢失你打好的内容

---

## v1.8.20

**主题：输入安全加固**

### 🐛 修复（安全加固）
- **私聊历史文件名安全化**：对方伪造的异常 ID 不再可能构造出越界的历史文件名（防御性加固）
- **在线名单字段限长**：对方发送的异常昵称 / 房间列表会被截断到合理长度，防止异常数据撑爆内存 / 界面

---

## v1.8.19

**主题：接收文件名安全化（路径穿越修复）**

### 🐛 修复（安全）
- **修复文件名路径穿越漏洞**：接收文件时，对方伪造的恶意文件名（如 `..\..\evil.exe`）会被安全化为纯文件名，防止把文件写到收件夹之外的位置

---

## v1.8.18

**主题：文件传输结果持久化 + 文档完善**

### ✨ 改进
- **文件传输结果持久化**：文件传输「失败 / 被拒」等结果现在会作为系统消息写入对应会话并计入未读，即使传输发生时你正在别的房间，切回来也能看到结果，不再静默丢失
- **文档完善**：修复 README 中一处截断的文案，并补充「发版需同步的版本号位置」「文件路径 / 大文件流式传输」等说明

---

## v1.8.17

**主题：连接状态提示优化**

### ✨ 改进
- **重连状态更清晰**：网络波动导致断线时，状态栏显示「连接断开，正在重连…」；自动重连成功后显示「已重新连接」，与首次「已连接」区分，不再让人误以为是永久掉线

---

## v1.8.16

**主题：一次发送多个文件**

### ✨ 改进
- **一次发送多个文件**：点「📎 文件/图片」现在可多选多个文件一次性发送，与拖拽发送一致

### 🧹 杂项
- 清理一处已废弃的内部方法

---

## v1.8.15

**主题：聊天区交互打磨**

### ✨ 改进
- **翻阅历史不再被打断**：新消息 / 新文件请求到达时，只有当你已经停在聊天区底部附近才会自动滚动到底；正在向上翻阅历史时不会被突然拉到最底
- **一键清空所有会话记录**：「帮助 → 清空所有会话记录」可一次性清空全部群聊 / 私聊记录（带确认，不可撤销）

---

## v1.8.14

**主题：文件消息增强（记录路径 + 右键打开位置）**

### ✨ 改进
- **文件消息记录保存路径**：发送 / 接收的文件消息现在会记住文件的本地路径（含历史记录），重启后依然能定位到文件
- **右键打开文件位置**：文件 / 图片消息右键新增「打开文件位置」（在资源管理器中定位并选中文件）和「复制路径」，无需再去收件夹里手动翻找

---

## v1.8.13

**主题：界面主题一致性打磨**

### 🎨 改进
- **图片预览窗口适配主题**：预览窗口不再固定深色背景，自动跟随暗色 / 浅色主题，浅色主题下不再出现突兀的深色窗口
- **「环境检测 / 关于」日志框适配主题**：安装日志框从固定黑底绿字改为跟随主题配色
- **弹窗支持 Esc 关闭**：「环境检测 / 关于」「发现新版本」弹窗现在也能按 Esc 关闭，与图片预览窗口一致

---

## v1.8.12

**主题：在线状态心跳保活（修复 TTL 误删回归）**

### 🐛 修复
- **在线状态心跳保活**：修复 v1.8.9 引入在线名单 TTL 后，长时间静默的活跃用户会被误判为下线的回归问题
- 现在客户端每 30 秒自动刷新一次自己的在线状态（心跳），保证「活跃即在线」，只有真正掉线（停止心跳）的成员才会被 TTL 清理

### 🧹 改进
- 在线名单清理更准确：既能兜底 will 消息丢失导致的「幽灵在线」，又不会误伤静默挂机的活跃用户

---

## v1.8.11

**主题：接收端流式落盘，双向大文件传输内存无忧**

### ⚡ 性能
- **接收端流式落盘**：接收文件时按分片直接写入临时文件（边收边写），收齐并校验通过后再改名成正式文件
- 内存占用从「整个文件大小」降到「单个分片大小」；配合 v1.8.10 的发送端流式读取，双向传输 200MB 大文件都不再占用数百 MB 内存

### 🐛 修复
- **修复发送空文件卡住**：发送 0 字节空文件时明确提示「文件为空」，避免流式发送在空文件上的边界问题
- 接收中断 / 超时 / 校验失败时自动清理残留临时文件，不留垃圾

---

## v1.8.10

**主题：文件发送流式读取，大幅降低内存占用**

### ⚡ 性能
- **文件发送流式读取**：发送文件时不再一次性把整个文件读进内存，改为按 256KB 分片边读边发，内存占用从「整个文件大小」降到「单个分片大小」
- 文件 MD5 校验改为流式计算，同样不再需要整文件常驻内存
- 效果：发送 200MB 大文件不再占用数百 MB 内存，低配机器也能稳定传输大文件

---

## v1.8.9

**主题：文件传输健壮性 + 在线名单清理**

### 🐛 修复
- **接收端传输看门狗**：文件接收若中途中断（发送方掉线 / 退出），120 秒后自动清理残留分片并提示「接收超时」，修复之前分片会一直占用内存不释放的隐患
- **分片索引越界防护**：接收数据时对分片序号做边界校验，越界 / 异常分片直接丢弃，防止异常数据把内存撑爆
- **文件请求参数一致性校验**：接收方校验文件大小与分片数的对应关系，不一致则拒绝，避免坏请求导致传输卡死

### 🧹 改进
- **在线名单 TTL 清理**：在线成员超过 120 秒未更新状态自动视为下线（后台每 30 秒巡检一次），兜底 will 消息丢失导致的「幽灵在线」——不再出现永远显示在线却收不到消息的假在线成员

---

## v1.8.8

**主题：彻底消除「控件销毁后迟到回调」报错**

### 🐛 修复
- 从源头根治 `bad window path name` 这类无害错误：给 tkinter 的 `focus_set` / `focus_force` 加了一层「控件已销毁则跳过」的兜底
  - 此前 v1.8.7 已做到「只记日志、不崩溃」，但 customtkinter 在 Windows 标题栏换色 / 最小化恢复时仍会 `after(1, widget.focus)` 延迟还原焦点，若界面此刻已重建、旧控件已销毁，仍会往 crash 日志里刷噪音
  - 现在这类迟到回调会在源头被静默拦截，不再产生任何日志、任何弹窗

---

## v1.8.7

**主题：修复切换主题崩溃**

### 🐛 修复
- 修复点击切换主题（顶部 ☀️/🌙 或「视图」菜单）后程序崩溃退出
  - 根因一：崩溃处理函数签名错误（多了一个 `self` 参数），导致 tkinter 回调里的异常被升级成整程序崩溃
  - 根因二：切换外观模式时，Windows 标题栏重绘会延迟 1ms 恢复焦点，此刻界面已重建、旧控件已销毁，触发「bad window path name」
  - 现改为：先重建界面、再切换外观模式；并对「控件销毁后的迟到回调」这类无害错误只记日志、不再弹「程序出错」误报

---

## v1.8.6

**主题：新消息提示音**

### ✨ 新增
- **新消息提示音**：收到消息时响一声（默认开启），可在「设置 → 新消息提示音」关闭，选择会记住

---

## v1.8.5

**主题：成员列表刷新性能优化**

### ⚡ 性能
- **成员列表防抖合并**：短时间内连串的成员上线/下线事件会合并成一次列表刷新，连接瞬间不再反复重建左侧栏、界面更流畅

---

## v1.8.4

**主题：彩色首字母头像 + 更早消息展开**

### ✨ 改进
- **彩色首字母头像**：未设置头像时，按昵称自动生成稳定配色的彩色首字母（Discord 风格），改名实时更新
- **「更早消息」点击展开**：聊天历史超过一屏时，点提示即可加载并展开全部更早消息

---

## v1.8.3

**主题：消息分组（Discord 式连续发言合并）**

### ✨ 改进
- **消息分组**：同一人在 5 分钟内的连续消息只显示一次名字和时间，气泡紧凑排列，聊天区更清爽（参考 Discord / QQ 设计）

---

## v1.8.2

**主题：启动自动连接 + 设置项修复**

### ✨ 新增
- **启动自动连接**：像 QQ / Discord 一样，启动后自动上线（默认开启），可在「设置 → 启动时自动连接」关闭，选择会记住

### 🐛 修复
- 修复设置项互相覆盖的隐患：切换主题会冲掉其它设置（现改为合并式写入）

---

## v1.8.1

**主题：补全暗色主题 + 安装器中文提示补全**

### ✨ 改进
- 「环境检测 / 关于」「发现新版本」两个弹窗也适配暗色 / 浅色主题，暗色下不再弹出刺眼的浅色窗口
- 安装器补全两条中文提示（数字签名校验提示），向导界面完全中文化

---

## v1.8.0

**主题：全新界面美化（暗色/浅色双主题）+ 中文安装程序**

### ✨ 新增 / 改进
- **全新主题系统**：默认暗色（参考 Discord），浅色参考 QQ；顶部 ☀️/🌙 一键切换，或「视图」菜单切换，选择自动保存、重启生效
- **界面全面美化**：消息气泡、文件请求卡片、系统提示、日期分隔线、会话列表、在线成员全部适配主题色，暗色下不再有刺眼白块
- **中文安装程序**：安装向导界面全中文（简体中文为默认语言），「下一步/安装/完成」等均为中文
- **头像占位**：未设置头像时显示 👤 占位图标，不再一片空白

### 🐛 修复
- 修复启动时主题设置不生效（之前写死浅色，与暗色配色体系冲突）

### ⚡ 性能
- 图片缩略图缓存（按文件+mtime 复用已解码图片，切会话/滚动不再反复解码大图，最多缓存 256 张）
- 会话搜索防抖（输入停顿 120ms 才刷新列表，不再每敲一个字符都重建整个列表）

---

## v1.7.1

**主题：修复头像保存失败 / 数据目录写入权限**

### 🐛 修复
- **修复「头像读取失败」的真正原因**：安装版装到 Program Files（普通用户只读）时，程序仍往 exe 目录写历史/头像导致写入失败。现在会自动探测目录是否可写，不可写时把数据改存到 `%APPDATA%\P2P聊天`，头像、昵称、ID、聊天记录都能正常保存

---

## v1.7.0

**主题：单窗口界面 + 应用内自动升级 + 安装器检测旧版本**

### ✨ 新增 / 改进
- **去掉独立的登录窗口**：启动后直接进入主界面；头像、昵称、用户 ID 都集成在主界面顶部，点头像即可换头像、昵称直接编辑、点「📋 复制ID」复制自己的 ID
- **应用内自动升级**：启动时后台检查 GitHub 最新版本，发现新版点「下载并安装」即可自动下载新版安装包并运行，无需每次手动更换安装程序
- **安装器检测旧版本**：安装时自动读取本机已安装的版本，并询问是否先卸载旧版本再覆盖安装（聊天记录不受影响）

### 🐛 修复 / 健壮性
- 修复进入聊天时崩溃退出的问题（去掉登录层过渡；并对所有图片强制解码校验，损坏图片不再触发渲染崩溃）
- 新增崩溃日志兜底：万一再遇到异常，会把详细错误写入本地 `crash.log` 并弹窗提示，便于定位问题

---

## v1.6.1

**主题：修复头像读取失败 + 进入聊天闪退**

### 🐛 修复
- 修复**头像读取失败**：选择损坏/截断/无法解码的图片时，不再把它复制成头像，而是明确拒绝并提示换图
- 修复**点击「进入聊天」闪退**：头像（或聊天里的图片）若损坏，之前会在渲染阶段触发底层崩溃导致程序闪退；现在加载时强制完整解码校验，损坏图片优雅降级为占位，不再崩溃
- 所有图片（头像、聊天图片、图片预览）统一强制解码校验，杜绝惰性解码带来的延迟崩溃

---

## v1.6.0

**主题：窗口标题实时状态 + 未读提醒**

### ✨ 新增
- 窗口标题实时显示连接状态与在线人数（「已连接 · N 人在线」/「未连接」）
- 有未读消息时，标题前加红点「●」并显示未读条数，切到会话或清空后自动消失

---

## v1.5.0

**主题：聊天记录管理**

### ✨ 新增
- 「帮助 → 清空当前会话记录」：一键清空当前群聊/私聊的全部记录（带确认，不可撤销）
- 私聊会话支持**右键删除**：删除会话并清除其全部聊天记录
- 清空/删除都会同步删除本地对应的历史文件，不留残留

---

## v1.4.0

**主题：内置图片预览 + 未读消息红色徽标**

### ✨ 新增
- 单击聊天里的图片，弹出**内置预览窗口**（等比放大、居中、Esc/关闭按钮退出），不再调用外部看图程序
- 会话列表未读消息改为**红色圆形数字徽标**（99 以上显示「99+」），群聊和私聊统一显示，一目了然

### 🔧 细节
- 私聊原来的「(n)」文本未读标识，统一升级为醒目徽标

---

## v1.3.0

**主题：消息时间戳 + 日期分隔线 + 消息复制**

### ✨ 新增
- 每条消息右下角显示发送时间（HH:MM）
- 聊天记录跨天时自动插入日期分隔线（「今天 / 昨天 / YYYY-MM-DD」）
- 右键单击任意消息气泡可复制文字；右键图片可复制其本地路径

### 📐 细节
- 旧历史消息若没有时间戳则自动隐藏时间显示（不报错、不占位），新消息全部带时间

---

## v1.2.0

**主题：修复登录框按钮被裁掉 + 登录简化 + 手动检查更新**

### 🐛 修复
- 修复「登录框底部『进入聊天』按钮被裁掉看不见」的问题
  - 根因：旧版登录窗口是固定尺寸且不可缩放，内容总高超过窗口高度，高分屏（125%/150% 缩放）下更严重，导致按钮被挤出屏幕
  - 现改为按真实内容自动适配窗口高度，按钮永远完整可见
- 头像尺寸恢复为与显示框一致（88×88），不再被拉伸或裁切

### ✨ 改进
- 登录流程更顺滑：不填昵称直接点「进入聊天」也会自动使用「未命名」，真正一键进入
- 新增「帮助 → 检查更新」菜单项：手动检查更新，状态栏会明确提示「已是最新版本 / 发现新版 / 检查失败」

### 🧹 杂项
- 新增本 `CHANGELOG.md`，以后每次发版都会同步记录

---

## v1.1.0

**主题：修复启动崩溃 + 内置自动更新 + 登录界面重做**

### 🐛 修复
- 修复「点击进入聊天后程序直接崩溃、不出窗口」
  - 根因：拖拽库 `tkinterdnd2` 的 `tkdnd` 组件在部分环境加载失败，主界面仍无条件注册拖拽目标，触发 `invalid command name "tkdnd::drop_target"`
  - 现改为运行时探测拖拽能力，失败则自动降级（不影响正常聊天），不再崩溃
- 修复 `_load_rooms` 中未定义变量 `p` 导致的房间列表读取异常

### ✨ 新增
- 内置自动检查更新：启动后静默查询 GitHub Releases，发现新版本弹窗提示（含版本号、更新内容、下载按钮）

---

## v1.0.0

初始版本：QQ 式单窗口多房间聊天、跨房间私聊、图片/文件传输、历史记录自动保存、环境自检一键安装。