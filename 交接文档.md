# P2P 聊天（P2PChat）交接文档

> 本文件供后续接手者（GLM5.4flash 等）快速了解项目结构、最近改动、已知问题与排查入口。
> 当前最新版本：**v3.3.3**（2026-08-29 发布，消息体验 5 连）。

## 一、项目概况

- 语言/技术：Python 3.13 + tkinter/customtkinter（GUI）、paho-mqtt、Pillow、tkinterdnd2、cryptography（可选 E2E）、sounddevice+numpy（语音）、qrcode+zxingcpp（名片码）、PyInstaller + Inno Setup 打包
- 形态：**单文件** `chat_gui.py`（约 8900 行）+ `chat.py`（旧 CLI 版，未维护）
- 定位：免服务器、免注册的跨公网聊天，走免费公共 MQTT broker 中转，支持多房间群聊、独立私聊、图片/文件/语音、局域网与公网 IPv6 直连加速
- 仓库：https://github.com/wayileina114-bit/p2p-chat （token 在 C:\Users\36087\Downloads\p2p\交接\github_token.txt，绝不入库）

## 二、消息通道架构（重要）

- 命名空间 `NS="p2pchat-opensquilla"`；主 broker `broker.emqx.io:1883`；辅助 broker `AUX_BROKERS`（hivemq/mosquitto/emqx）
- **多通道发布 `_publish_all(topic, payload, qos, retain, from_outbox)`**：主 + 所有在线辅助客户端都发；全部失败时入 `_outbox` 补发队列（容量 200），任一通道恢复后 `_flush_outbox` 自动重发
- 主题：
  - 群聊消息 `{NS}/rooms/{room}/msg`
  - 私聊 `{NS}/dms/{cid}`
  - 文件控制 `{NS}/rooms/{room}/file/ctrl` 与 `{NS}/dmfiles/{cid}/file/ctrl`；文件数据 `…/file/data`
  - 在线状态 `{NS}/presence/{cid}`（retain，TTL 120s，每 30s 心跳刷新）
- 去重：文本消息用 `_check_seen(mid)`；控制类（撤回/已读/回应/编辑/文件 offer）用 `_check_ctrl_seen(kind, mid)`（`_ctrl_seen` 集合）
- **文件传输（v3.3.2 重构）**：offer/accept/分片全部走多通道发布；分片 `from_outbox=True`（**绝不进文字补发队列**——否则 512KB 分片会塞满 outbox 拖垮文字消息并撑爆内存，这是 v3.3.2 修的核心崩溃/卡顿根因）；接收端按分片序号（`raw[13:17]` 大端 idx）去重；接收方每 6 秒发 `{"kind":"req","id","to","idx":[…]}` 请求缺失分片，发送方 `_resend_chunks` 只补缺失部分；发送方 10 秒未收到 accept 自动重发 offer，60 秒仍未应答才取消
- 分片帧格式：`b"C" + tid(12字节ascii) + struct.pack(">I", idx) + 分片内容`；分片大小 `CHUNK_SIZE=512KB`；加密时整片 Fernet 加密

## 三、直连加速

- 局域网：UDP 47655 发现广播 + TCP 47656 常驻消息 + 47654 文件直连（`_lan_discover_loop` / `_lan_msg_listener` / `_lan_listener`）
- 公网 IPv6：`_get_global_v6()` 过滤 ULA/fe80/::1/::ffff，presence 携带 `v6`，文件 offer 携带 `v6/v6_port`；`_try_lan_receive(..., is_v6)` 优先 IPv6 直连，失败回退 MQTT
- 防火墙：`_try_firewall_rule`（后台自动，`text=False`）与菜单「开放直连端口」（`_open_firewall_ports`，v3.3.2 修复 GBK 解码崩溃：**子进程输出必须 bytes 模式**，中文系统下 text 模式会 UnicodeDecodeError）

## 四、界面要点（聊天区）

- 消息渲染：`_render_feed`（清空+分批 `_render_feed_batch` 每批 25 条）→ `_add_bubble`（文字）/ `_add_image_bubble` / `_add_voice_bubble`；`RENDER_MAX=200`、`FEED_MAX=400`
- **悬停快捷浮层**（v3.3.0）：`_hover_enter/_leave/_show_hover_bar/_destroy_hover_bar`；同屏只存在一个，懒创建；文字/图片/语音气泡都支持（`_bind_hover` 绑定 Enter/Leave，add="+" 与既有绑定共存）；按钮：👍 快捷回应 / ↩ 引用 / ⧉ 多选转发（预选）/ 📋 复制
- **回应芯片**（v3.3.0）：`_build_reaction_row` 生成可点击芯片（点击切换自己的回应，我回应过的高亮 accent），行尾 ＋ 打开快捷表情面板 `_show_quick_reactions`
- **消息详情**（v3.3.0）：`_show_message_details`——点「已读/已送达」标签或右键回应芯片，看已读/送达/各表情名单
- **任务栏角标**（v3.3.0）：`_update_taskbar_badge` → `_build_badge_icon`（PIL 画红底数字存 ICO）+ `_taskbar3_set_overlay`（ctypes COM ITaskbarList3.SetOverlayIcon）；未读为 0 清角标，替换前 DestroyIcon，退出 `_taskbar3_release`
- **长消息折叠**（v3.3.3）：>320 字折叠为预览 + 「展开全文」，`_expanded_msgs` 记住已展开
- **@我汇总**（v3.3.3）：标题栏 `📢 @我(N)` 按钮 → `_open_mentions` 列出所有会话 @我的消息，点击 `_jump_mention` 跳转
- **消息筛选**（v3.3.2）：标题栏「🖼 图片」「📎 文件」切换 `_feed_filter`（可与搜索叠加）
- **主题**：深/浅/跟随系统（v3.3.2 新增 `_detect_system_theme` 读注册表 AppsUseLightTheme；设置中心外观三选 `_apply_appearance_mode`）；切换走 `_rebuild_ui`（先隐藏窗口、清 `_images`、`RENDER_MAX=60` 后补全）
- **搜索**：会话内 Ctrl+F（`_open_search`，Enter/Shift+Enter/▲▼ 跳转命中 `_jump_search_hit`）；全局 Ctrl+Shift+F（`_open_global_search` 跨会话，点击跳转并复用会话内搜索高亮）

## 五、已知问题 / 排查入口（重要）

1. **`_smoke_test.py` 的 LAN auto-discover 偶发失败**：环境性抖动（UDP 47655 端口被残留进程占用 / 时序），不是产品 bug。跑前先：
   `Get-NetUDPEndpoint -LocalPort 47655 | Stop-Process`，必要时杀残留 python。已加大等待与重发次数，仍偶发。
2. **公共 MQTT broker 可靠性**：emqx.io 高峰期拥堵会导致消息延迟/文件分片丢失。多通道 + 分片补发已大幅缓解；若再遇「传输失败」先看状态栏报错文案与 `%LOCALAPPDATA%\P2PChat\crash.log`。
3. **崩溃日志位置**：`%LOCALAPPDATA%\P2PChat\crash.log`（主线程回调异常会弹「程序出错」窗并写日志；后台线程异常只写日志不弹窗）。
4. **性能注意**：`_apply_session_list` 每次变化全量重建左侧列表（有指纹跳过 `_list_fingerprint`）；`_refresh_peers` 已 200ms 防抖（v3.3.2）。新增每消息级别的控件/计时器要评估数量。
5. **版本号需同步 5 处**：`chat_gui.py` APP_VERSION、`CHANGELOG.md`、`README.md`、`installer\p2pchat.iss`、`version_info.txt`；发布说明在 `_release_notes.md`（不入库）。

## 六、构建与发布流程

1. 冒烟测试：`python -u _smoke_test.py`（后台跑 + 读日志更稳；当前 100+ 项）
2. 打包：`python -m PyInstaller --noconfirm --onedir --windowed --name P2PChat --icon P2PChat.ico --add-data "P2PChat.ico;." --version-file version_info.txt --collect-all tkinterdnd2 --collect-all customtkinter --collect-all sounddevice --collect-all qrcode --collect-all zxingcpp chat_gui.py`
3. 安装包：`& 'C:\Users\36087\AppData\Local\Programs\Inno Setup 6\ISCC.exe' installer\p2pchat.iss` → `installer\P2PChat-Setup.exe`
4. Git：`git add -A && git commit -m "vX.Y.Z: …" && git tag vX.Y.Z`；push：`git push https://x-access-token:<tok>@github.com/wayileina114-bit/p2p-chat.git main --tags`（若 amend 过提交，先删远端同名 tag 再推）
5. Release：`python 'C:\Users\36087\Downloads\p2p\交接\release_helper.py' <tok> <ver> <repo_root>`（读 `_release_notes.md`，自动建 Release + 上传 Setup.exe）

## 七、版本历史（最近）

- **v3.3.3**：长消息自动折叠（>320 字 + 展开全文）；复制为引用；@我汇总面板（标题栏 📢 @我(N) 点击跳转）；图片右键直接转发；接收文件进度实时显示
- **v3.3.2（稳定修复版）**：文件传输多通道 + offer 重发 + 缺失分片补发；修复子进程 GBK 解码崩溃；分片不再塞满文字补发队列；在线状态防抖；图片预览置顶
- **v3.3.1**：全局快捷键（Ctrl+Shift+F 全局搜索 / Ctrl+N 私聊 / Ctrl+R 重连 / Alt+↑↓ 切会话）；悬停浮层覆盖图片/语音；搜索命中跳转；全部已读；文件「打开文件夹」按钮
- **v3.3.0**：消息悬停快捷浮层；任务栏未读角标；回应芯片可点击；会话按最近活跃排序；消息详情弹窗

## 八、待办 / 方向建议

- v3.3.3 收尾（长消息折叠/复制为引用/@我汇总/图片转发/接收进度，测试已基本就绪）
- 可继续：断线重连后的消息补齐、群聊成员管理、输入框富文本、传输队列可视化管理
